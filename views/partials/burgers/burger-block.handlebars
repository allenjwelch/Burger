<li>
	{{burger_name}}

	<button class="change-sleep" data-id="{{id}}" data-devour="{{devoured}}">
		{{#if devoured}}Hungry?{{else}}Eat it again!{{/if}}
	</button>

	<button class="delBurger" data-id="{{id}}">DELETE</button>

</li>
